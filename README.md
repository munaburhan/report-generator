# Report Generator (YAML → Markdown)

A simple automation project that generates a Markdown report from a YAML file using Python and GitHub Actions.

## How it works
- Edit `data.yml`
- GitHub Actions runs automatically
- `REPORT.md` is regenerated and committed

## Tech stack
- Python
- PyYAML
- GitHub Actions (CI)
